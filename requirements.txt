# Core AutoGen dependencies (updated for v0.7.4+ with memory-based RAG)
autogen-agentchat>=0.7.4
autogen-ext[openai]>=0.7.4
autogen-core>=0.7.4

# OpenAI API client (required by AutoGen)
openai>=1.93.0
tiktoken>=0.8.0

# YAML processing (required for team configuration files)
pyyaml>=6.0

# PDF processing and RAG dependencies
pypdf>=6.0.0
chromadb>=1.0.0
sentence-transformers>=5.1.0

# Optional document processing
python-docx>=1.1.0

# NumPy compatibility fix for ChromaDB
numpy<2

# Web scraping and HTTP requests
httpx>=0.24.0
beautifulsoup4>=4.12.0
html2text>=2020.1.16

# YAML processing
PyYAML>=6.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: Code formatting and linting
black>=23.0.0
flake8>=6.0.0

# Optional: Jupyter notebook support (if needed for development)
jupyter>=1.0.0
ipykernel>=6.0.0
